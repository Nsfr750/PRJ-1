Per iniziare:
-------------
py -m venv venv
.\venv\Scripts\activate
py -m pip install --upgrade pip
py -m pip install -r requirements.txt

open and show the todo list


Nuitka

--standalone                    # Crea eseguibile standalone
--onefile                       # Crea file singolo
--follow-imports               # Segue tutte le importazioni
--include-package=PySide6       # Include PySide6
--include-package=script       # Include package script
--plugin-enable=pyside6         # Abilita plugin PySide6
--windows-disable-console      # Disabilita console (Windows)
--output-dir=dist              # Directory output
--remove-output                # Rimuove file temporanei
